# Stub for SaveService — kept empty so SaveService doesn't try to register
# legacy XStream aliases against classes we don't load. Tests don't call
# SaveService.save/load directly, so an empty file is sufficient.
_version=4.0
